Navigate to this folder using terminal command-cd, or control+click on this folder's icon the in the tree path, and select open in terminal.
run the commands:
1)virtualenv venv
2)source venv/bin/activate
3)pip install -r requirements.txt
4)python3 main.py
and your app is ready to run